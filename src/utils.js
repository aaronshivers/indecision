console.log('utils.js running');

export const squareIt = (x) => x ** 2;
export const add = (x, y) => x * y;
export const multiply = (x, y) => x * y;
export default (x) => x >= 65;

// export { add, squareIt, multiply as default };
